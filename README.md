 # Demo project
This is our first group collaboration  
<!-- # Backend — Event Registration & Tracking System

## Setup
1. `npm install`
2. Create `.env` (see `.env.example`)
3. `npm run dev`

## Structure
- `routes/` — endpoint definitions
- `controllers/` — request handling + DB queries
- `middleware/` — auth + error handling
- `utils/` — helpers (tracking ID generation, validation)

## API Docs
See Postman collection: [link if you export/share one] --> -->